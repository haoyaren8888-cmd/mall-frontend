# 综合网上购物商城前端

Vue3 + Vite + Element Plus + Pinia + Axios。

## 启动

```bash
npm install
npm run dev
```

默认访问：`http://localhost:5173`

后端接口默认地址：`http://localhost:8080/api`。如需修改，可以设置环境变量 `VITE_API_BASE_URL`。
